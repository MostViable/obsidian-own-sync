[package]
name = "own-sync-server"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
axum = "0.8.9"
serde = { version = "1", features = ["derive"] }
tokio = { version = "1", features = ["macros", "net", "rt-multi-thread"] }
